/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com'], // Add the hostname here
        domains: ['cdn-icons-png.flaticon.com'], // Add the hostname here
      },
};

export default nextConfig;
